
export const MIN_LENGTH_INPUT = 2

export enum ErrorMessages {
    NotFound = "Usuario no encontrado.",
    General = "Ha ocurrido un error. Vuelve a intentarlo.",
    MinLength = `Ingresa mínimo ${MIN_LENGTH_INPUT} caracteres.`
}
